# Specs

Feature specification documents go here, one per feature.

## Format

`.github/specs/<feature>.md`

## Lifecycle

1. `analyst` agent writes the spec
2. User reviews and approves (or requests changes)
3. `implementer` agent reads the spec and builds the module
4. `reviewer` agent validates implementation against the spec
