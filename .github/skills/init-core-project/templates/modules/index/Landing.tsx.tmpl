import React, { useState, useCallback } from "react";
import { createRoot } from "react-dom/client";

// ── Types ──────────────────────────────────────────────────────
declare global {
  interface Window {
    __INITIAL_DATA__?: LandingData;
  }
}

interface LandingData {
  title?: string;
  version?: string;
  description?: string;
}

// ── Theme ──────────────────────────────────────────────────────
function toggleTheme() {
  const h = document.documentElement;
  h.classList.toggle("light");
  h.classList.toggle("dark");
  const mode = h.classList.contains("light") ? "light" : "dark";
  try { localStorage.setItem("theme", mode); } catch { /* ignore */ }
}
function getInitialTheme(): boolean {
  if (typeof window === "undefined") return false;
  try { return localStorage.getItem("theme") === "light"; } catch { return false; }
}

// ── Icons ──────────────────────────────────────────────────────
const Sun = () => (
  <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.5" width="15" height="15">
    <path strokeLinecap="round" strokeLinejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
  </svg>
);
const Moon = () => (
  <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.5" width="15" height="15">
    <path strokeLinecap="round" strokeLinejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
  </svg>
);
const GithubIcon = () => (
  <svg className="w-4 h-4 shrink-0" fill="currentColor" viewBox="0 0 24 24">
    <path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z" />
  </svg>
);
const BookIcon = () => (
  <svg className="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2">
    <path strokeLinecap="round" strokeLinejoin="round" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
  </svg>
);

// ── Step data ──────────────────────────────────────────────────
const steps = [
  { num: "1", title: "Scaffold a module", code: "make scaffold" },
  { num: "2", title: "Auto-registered", code: "init() auto-registers the module" },
  { num: "3", title: "Run the server", code: "make run" },
];

// ── Landing Page Component ─────────────────────────────────────
export function Landing() {
  const [light, setLight] = useState(getInitialTheme);

  const toggle = useCallback(() => {
    toggleTheme();
    setLight((prev) => !prev);
  }, []);

  return (
    <>
      {/* ── Header ── */}
      <header className="sticky top-0 z-20 flex items-center justify-between h-12 sm:h-14 px-4 sm:px-8 bg-bg-primary/75 backdrop-blur-lg">
        <a href="/" className="flex items-center gap-2.5 no-underline">
          <span className="flex items-center justify-center w-7 h-7 rounded-lg bg-accent text-white text-xs font-bold">F</span>
          <span className="text-sm font-semibold tracking-tight text-text-primary">flow</span>
          <span className="text-[10px] font-semibold uppercase tracking-wider px-1.5 py-0.5 rounded-full bg-accent/10 text-accent">v0.1.0</span>
        </a>
        <button onClick={toggle} className="flex items-center justify-center w-8 h-8 rounded-lg border border-text-muted/20 bg-transparent text-text-tertiary cursor-pointer transition-all hover:bg-accent-subtle hover:border-accent hover:text-accent" title="Toggle theme">
          {light ? <Sun /> : <Moon />}
        </button>
      </header>

      {/* ── Main ── */}
      <main className="relative z-10 flex-1 flex flex-col items-center justify-center px-5 py-8 sm:py-12 lg:py-16">
        <div className="w-full max-w-sm md:max-w-md flex flex-col items-center">

          {/* Hero */}
          <div className="flex flex-col items-center text-center anim-up d1">
            <span className="flex items-center justify-center w-12 h-12 rounded-2xl bg-accent/10 text-accent text-lg mb-6">✦</span>
            <h1 className="text-2xl sm:text-3xl lg:text-4xl font-bold tracking-tight leading-tight mb-3">
              Welcome to <span className="gradient-text">Flow</span>
            </h1>
            <p className="text-sm sm:text-base text-text-tertiary max-w-xs mx-auto">Your app is running. Scaffold your first module to begin.</p>
          </div>

          <div className="h-4 sm:h-6" />

          {/* Status */}
          <div className="flex items-center gap-2.5 anim-up d2">
            <span className="status-dot" />
            <span className="text-xs font-medium text-text-tertiary uppercase tracking-[0.15em]">Online</span>
            <span className="w-px h-3.5 bg-border-subtle" />
            <span className="text-[11px] font-mono text-text-tertiary/40">v0.1.0</span>
          </div>

          <div className="h-4 sm:h-6" />

          {/* Steps */}
          <div className="glass-card rounded-2xl w-full anim-up d3">
            <div className="divide-y divide-border-subtle/30">
              {steps.map((s) => (
                <div key={s.num} className="flex items-start gap-4 px-5 py-3 sm:px-6 sm:py-4">
                  <span className="flex items-center justify-center w-7 h-7 rounded-lg bg-accent/10 text-accent text-[11px] font-semibold shrink-0 mt-0.5">{s.num}</span>
                  <div className="min-w-0">
                    <p className="text-sm font-medium text-text-primary">{s.title}</p>
                    <p className="mt-1 text-xs font-mono text-text-tertiary/60">{s.code}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>

          <div className="h-4 sm:h-6" />

          {/* CTA */}
          <div className="flex flex-col sm:flex-row items-center gap-3 w-full sm:w-auto anim-up d4">
            <a href="https://github.com/ynwd/flow" target="_blank" className="inline-flex items-center justify-center gap-2 w-full sm:w-auto h-10 sm:h-11 px-6 sm:px-7 rounded-xl bg-accent text-white text-sm font-medium no-underline transition-all hover:opacity-85 active:scale-[.97]">
              <GithubIcon />
              Repository
            </a>
            <a href="https://go.dev" target="_blank" className="inline-flex items-center justify-center gap-2 w-full sm:w-auto h-10 sm:h-11 px-6 sm:px-7 rounded-xl border border-border-subtle bg-transparent text-text-secondary text-sm font-medium no-underline transition-all hover:bg-accent-subtle hover:border-accent hover:text-accent active:scale-[.97]">
              <BookIcon />
              Docs
            </a>
          </div>

        </div>
      </main>

      {/* ── Footer ── */}
      <footer className="relative z-10 flex items-center justify-between h-12 sm:h-14 px-4 sm:px-8">
        <span className="text-xs text-text-tertiary/60">&copy; {new Date().getFullYear()} Flow</span>
        <div className="flex items-center gap-5">
          <a href="https://go.dev" target="_blank" className="text-xs text-text-tertiary/60 no-underline transition-colors hover:text-text-primary">Go</a>
          <a href="https://react.dev" target="_blank" className="text-xs text-text-tertiary/60 no-underline transition-colors hover:text-text-primary">React</a>
          <a href="https://tailwindcss.com" target="_blank" className="text-xs text-text-tertiary/60 no-underline transition-colors hover:text-text-primary">Tailwind</a>
        </div>
      </footer>
    </>
  );
}

// ── Mount ──────────────────────────────────────────────────────
const el = document.getElementById("index-root");
if (el) {
  const root = createRoot(el);
  root.render(<Landing />);
}
